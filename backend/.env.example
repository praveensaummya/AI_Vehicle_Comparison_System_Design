# AI Vehicle Comparison System - Environment Configuration

# LLM Provider Configuration
# Options: "gemini" (recommended, free), "openai" (paid), "mock" (testing)
LLM_PROVIDER=gemini

# Google Gemini API Configuration (Recommended - FREE with generous quotas)
# Get your free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSyDiJfKjDOWRUimepaH9yEiL3sX9W9F4Wyw
GEMINI_MODEL=gemini-1.5-flash

# OpenAI Configuration (Legacy support - PAID service)
OPENAI_API_KEY=your_openai_api_key_here

# Serper API Configuration (for web search)
SERPER_API_KEY=187f322c2d175d91aadc5c7677d023070b998ce1

# Mock Mode Configuration
# Set to "true" to use mock crew instead of real API calls
# Useful for testing and development
USE_MOCK_CREW=false

# System will automatically select the best provider:
# 1. If USE_MOCK_CREW=true: Always use mock responses
# 2. If LLM_PROVIDER=gemini: Use Gemini API (free, generous quotas)
# 3. If LLM_PROVIDER=openai: Use OpenAI API (paid service)
# 4. Fallback: Mock responses if no valid API keys found
