# Serper API Configuration (for web search)
SERPER_API_KEY=sajugsaugduagbfgaudfgauifg

# Google Gemini API Configuration (Primary)
# REPLACE WITH YOUR NEW PAID API KEY FROM: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=liashdiadioahdihaihdaihda
# LLM Provider Configuration
LLM_PROVIDER=gemini
GEMINI_MODEL=gemini-1.5-flash

# Mock Mode Configuration (set to false for production)
USE_MOCK_CREW=false

# Ensure Google AI Studio (not Vertex AI) is used
GOOGLE_APPLICATION_CREDENTIALS=
